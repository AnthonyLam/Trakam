package com.trakam.trakam.util

object PrefKeys {
    object Server {
        val KEY_SERVER_HOST = "pref_key_server_host"
        val KEY_SERVER_PORT = "pref_key_server_port"

        object Default {
            val SERVER_HOST = "192.168.0.189"
            val SERVER_PORT = "8080"
        }
    }
}